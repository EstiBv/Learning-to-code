"use strict";
const list = document.querySelector(".js-list");
const cities = document.querySelectorAll(".js-item");

function selectCity(ev) {
  const clickItems = ev.currentTarget;
  console.log(clickItems);
}
if (value === "1") {
}

list.addEventListener("click", selectCity);
